streamlit>=1.40
pandas>=2.0
python-frontmatter>=1.0
python-dotenv>=1.0
watchdog>=4.0
openpyxl>=3.1
python-docx>=1.1
python-pptx>=0.6.21
psutil>=5.9
plotly>=5.0
yfinance>=0.2.40
requests>=2.31
fredapi>=0.5
icalendar>=5.0
recurring-ical-events>=2.1
httpx>=0.27
authlib>=1.3
krakenex>=2.1

# Phase A — capture pipeline OCR. Needs the Tesseract binary on the machine
# (winget install UB-Mannheim.TesseractOCR) plus the German language pack
# (deu.traineddata). Pillow loads the captured image. Whisper (voice STT) is the
# same dependency the Jarvis voice layer already declares in backend/requirements.txt.
pytesseract>=0.3.10
Pillow>=10.0
