# Backend-specific dependencies (Phase 11 FastAPI layer).
# Most data/finance deps (pandas, yfinance, openpyxl, frontmatter, psutil, ...)
# already live in the project-root requirements.txt — install that too.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
websockets>=12

# Phase 13 — Jarvis voice layer (local STT + wake word).
# Whisper also needs FFmpeg on PATH (winget install ffmpeg) and, on first use,
# downloads its ~150 MB "base" model. Piper (TTS) is a standalone binary placed
# in backend/voice/piper/ — see README.
openai-whisper>=20231117
pvporcupine>=3.0.0  # wake word, free for personal use with an access key

# Phase 15a — finance foundation (performance + risk analytics).
# NOTE: the original `empyrical` (0.5.5) fails to build on Python 3.12 (its
# bundled versioneer uses the removed configparser.SafeConfigParser).
# `empyrical-reloaded` is the maintained drop-in fork and imports under the SAME
# `empyrical` namespace, so all module code (`import empyrical as ep`) is unchanged.
empyrical-reloaded>=0.5.11
quantstats>=0.0.62
scipy>=1.11
numpy-financial>=1.0.0
# (yfinance, pandas, pyarrow already present in the project-root requirements.txt)
