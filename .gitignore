__pycache__/
*.pyc
*.pyo
.env
.streamlit/secrets.toml
data/cache/

# Phase 15a finance foundation — generated/personal financial data (per-machine).
data/positions.json
data/transactions.jsonl
data/price_cache/

# Phase 15b — Fama-French factor data cache (downloaded from Ken French library).
data/ff_cache/

# Item #16 weekly briefing — per-machine scheduler state (last trigger fired).
data/weekly_briefing_state.json

# System page — Skills & Agents registry soft-disable/archive state (per-machine).
data/registry_state.json

# Wealth page (v2 N1) — monthly net-worth snapshot store + personal wealth
# reference data (insurance / PKV / accounts). Per-machine, never committed.
data/networth_snapshots.json
data/wealth_config.json
.venv/
venv/
.DS_Store
Thumbs.db
*.bak

# Phase 13 voice — Piper binary + voice models live in backend/voice/piper/
# but are large and downloaded per-machine (see backend/voice/piper/README.md).
backend/voice/piper/*
!backend/voice/piper/README.md
